{
  "name": "minutemaster-client",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "serve -p 8080",
    "dev": "serve -p 8080",
    "generate-config": "node generate-config.js",
    "build": "npm run generate-config"
  },
  "devDependencies": {
    "serve": "^14.2.1"
  }
}

